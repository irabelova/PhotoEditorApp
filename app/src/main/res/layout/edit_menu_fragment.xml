<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/draw_button"
        style="@style/Buttons"
        android:drawableTop="@drawable/ic_draw"
        android:text="@string/draw"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/set_filter_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/set_filter_button"
        style="@style/Buttons"
        android:drawableTop="@drawable/ic_photo_filter"
        android:text="@string/filters"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/crop_button"
        app:layout_constraintStart_toEndOf="@id/draw_button"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/crop_button"
        style="@style/Buttons"
        android:drawableTop="@drawable/ic_crop"
        android:text="@string/crop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/set_filter_button"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>