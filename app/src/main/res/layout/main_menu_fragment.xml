<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_orange"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        android:id="@+id/make_photo_button"
        style="@style/Widget.MaterialComponents.TimePicker.ImageButton"
        android:backgroundTint="@color/dark_orange"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:text="@string/make_photo"
        app:layout_constraintBottom_toTopOf="@id/photo_from_device_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/photo_from_device_button"
        style="@style/Widget.MaterialComponents.TimePicker.ImageButton"
        android:backgroundTint="@color/dark_orange"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:text="@string/upload_photo_from_device"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/make_photo_button"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>